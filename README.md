# Blockhouse-Trial-Task

Download Order_Flow_Imbalances.ipynb and first_25000_rows.csv to a folder. Run each cell in the python notebook to get the OFI output (Best-Level OFI, Multi-Level OFI, Integrated OFI Cross-Asset OFI). In the dataset provided (csv file), there were only 5000 rows instead of 25000 and only one ticker (AAPL). Thus computing Cross-Asset OFI was not possible.

Blockhouse_Conceptual_Questions.pdf contains the solutions to the conceptual questions in a latex structured format.
